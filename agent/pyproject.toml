[project]
name = "personal-agent-agent"
version = "0.1.0"
description = "Agent runtime for Personal Agent"
dependencies = [
    "Flask",
    "httpx",
    "langchain-core",
    "langgraph",
    "pgvector",
    "psycopg[binary]",
    "SQLAlchemy",
]
