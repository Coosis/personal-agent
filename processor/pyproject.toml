[project]
name = "personal-agent-processor"
version = "0.1.0"
description = "Document processing pipeline for Personal Knowledge Base Agent"
dependencies = [
    "httpx",
    "langchain-text-splitters",
    "pgvector",
    "psycopg[binary]",
    "SQLAlchemy",
    "tenacity",
    "unstructured",
]
